<%= form_for objects, :html => {:class => 'new_slide', :multipart => true } do |f| %>
  <div class='actions'>
    <%= link_to image_tag('image_new.gif'), '#', :class => 'upload_photo' %><br/>
    <%= submit_tag I18n.t('send'), :class => 'btn primary' %><span class='loading'><%= image_tag('loading-data.gif', :style => 'display:none;') %></span>
  </div>
<% end %>